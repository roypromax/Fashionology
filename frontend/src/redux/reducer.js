const initialState = {
  products: [],
  product: {},
  loading: false,
};

const productsReducers = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default productsReducers;
